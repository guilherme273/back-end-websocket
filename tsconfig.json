{
  "compilerOptions": {
    /* Projetos */
    // "incremental": true,  /* Salva arquivos .tsbuildinfo para compilação incremental. */
    // "composite": true,    /* Habilita restrições que permitem usar referências de projeto. */
    
    /* Linguagem e Ambiente */
    "target": "es2016",  /* Define a versão do JavaScript gerado. */
    "module": "commonjs", /* Especifica o formato do módulo gerado (Node.js usa 'commonjs'). */
    
    /* Diretórios */
    "rootDir": "./src",   /* Diretório onde seus arquivos TypeScript estão. */
    "outDir": "./dist",   /* Diretório onde os arquivos compilados JavaScript serão colocados. */

    /* Interoperabilidade */
    "esModuleInterop": true, /* Facilita a importação de módulos CommonJS. */
    "forceConsistentCasingInFileNames": true, /* Garante que a capitalização dos arquivos seja consistente. */
    
    /* Checagem de tipos */
    "strict": true,  /* Ativa todas as opções rigorosas de verificação de tipos. */
    "skipLibCheck": true, /* Ignora a verificação de tipos nas bibliotecas. */

    /* Emissão de Arquivos */
    "sourceMap": true,   /* Gera arquivos de mapa de origem para depuração. */
    "removeComments": true, /* Remove comentários do código gerado. */
    
    /* Outros ajustes */
    "resolveJsonModule": true, /* Permite importar arquivos JSON. */
    "noEmitOnError": true,     /* Impede a geração de arquivos se houver erros de tipo. */

    /* Ajustes para importações e módulos */
    "moduleResolution": "node", /* Resolução de módulos compatível com Node.js. */

    /* Outras opções úteis */
    // "noImplicitAny": true, /* Ativa erros para variáveis com tipo 'any' implícito. */
    // "strictNullChecks": true, /* Verifica 'null' e 'undefined' explicitamente. */
  },

  /* Arquivos incluídos e excluídos */
  "include": [
    "src/**/*.ts"  /* Inclui todos os arquivos TypeScript dentro da pasta src. */
  ],
  "exclude": [
    "node_modules" /* Exclui a pasta node_modules do processo de compilação. */
  ]
}
